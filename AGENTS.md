---
project: web3-study
language: zh-CN
scope: main
---

# 项目规范（主项目·Web3）

本规范用于主项目的统一约束，适用于所有 Web3 相关代码与文档产出。强调 **安全优先、最小权限、零冗余、强业务对齐、可审计**。当模块内有更严格规范时，以更严格者为准。

## 0. 规范源自动加载（强制）
为便于跨项目复用，本节作为“可复制模板”固定放在文件开头。新项目只需复制本节，再补充项目特有规范。

### 0.1 默认加载源
- 主规范：`AGENTS.md`（本文件，始终生效）。
- Cursor 规则：`.cursor/rules/*.mdc`（按规则声明执行，`alwaysApply: true` 视为全局强制）。
- Cursor Skills：`.cursor/skills/**/SKILL.md`（按任务匹配自动启用；用户显式点名时必须启用）。
- Cursor Comment/Command/Agent 约束（若存在）：
  - `.cursor/comments/**/*`
  - `.cursor/commands/**/*`
  - `.cursor/agents/**/*`
  以上作为补充规范源，涉及代码风格、审查清单、交付格式时应同时遵循。

### 0.2 Claude/Gemini 兼容加载
若仓库中存在以下文件，视为可自动兼容的补充规范源，无需额外人工声明：
- `CLAUDE.md`、`CLAUDE.local.md`、`.claude/**/*.md`
- `GEMINI.md`、`GEMINI.local.md`、`.gemini/**/*.md`
- `.cursorrules`（若存在）

### 0.3 冲突处理优先级
当多规范冲突时，按以下顺序执行（高 -> 低）：
1. 安全与合规红线（任何来源中的更严格条款）
2. 本文件 `AGENTS.md`
3. `.cursor/rules/*.mdc`
4. `.cursor/skills/**/SKILL.md`
5. `CLAUDE* / GEMINI* / .claude / .gemini / .cursorrules`
6. 其他辅助文档（`.cursor/comments`、`.cursor/commands`、`.cursor/agents`）

### 0.4 执行约束
- 未读取到某规范文件时，不阻塞执行；但需在输出中明确“未发现对应文件，已按可用规范执行”。
- 对同一任务，若多份规范可用，必须选择“更严格且更贴近任务域”的规则集合。
- 新增或更新规范文件后，下一次会话默认按最新版本执行。

## 1. 总体原则
- 安全优先：默认防御性编程，零容忍已知漏洞。
- 最小权限：进程、函数、合约、配置与密钥访问均最小化。
- 代码极简：拒绝空逻辑、死代码、无效判断与重复实现。
- 可审计性：结构清晰、注释完善、命名语义化。
- 业务对齐：代码必须严格匹配 Web3 业务规则，不添加多余功能。

## 2. 通用安全规范（强制）
- 输入校验：所有外部输入（HTTP/RPC/CLI/文件）必须校验类型、长度、范围、格式（地址、哈希、金额、时间等）。
- 敏感数据：私钥/助记词/API Key 禁止明文存储与日志输出；使用后必须尽可能清理内存或生命周期。
- 访问控制：权限校验前置，统一入口处理；禁止硬编码凭证/地址。
- 外部依赖：只使用维护活跃且可信库；版本精确锁定；定期做漏洞扫描。
- 错误处理：所有错误必须处理并保留上下文；禁止静默忽略。

## 3. 语言规范（按技术栈适用）
### 3.1 Go 代码规范（适用于链下服务/SDK）
- Go 版本：仅允许 Go 1.21+，`go.mod` 精确版本。
- 结构限制：单文件 ≤500 行，单函数 ≤50 行，嵌套 ≤2 层。
- 注释与命名：导出项必须有 Godoc；关键逻辑说明“为什么这样做”；命名语义化。
- 并发安全：共享变量必须同步；使用 `context` 管理协程；禁止无控协程。
- RPC/HTTP：使用连接池；禁止每次新建连接；敏感信息不可写入日志。

### 3.2 Solidity 规范（适用于合约）
- Solidity 版本：仅允许 `0.8.20+`，且必须精确锁定（禁止 `^`）。
- 结构顺序固定：注释头 → 版本 → 导入 → 错误 → 常量 → 状态变量 → 修饰器 → 事件 → 函数。
- 安全强制：
  - 重入防护 + Checks-Effects-Interactions。
  - 禁止 `tx.origin` 权限校验、禁止 `selfdestruct`。
  - 转账使用 `call` 并检查返回值；ERC20 使用 `safeTransfer`。
- 事件与校验：所有状态变更必须发事件；外部函数参数必须校验并使用自定义错误。

## 4. 业务与链上交互规范
- 链上参数必须与链协议规范一致（如 JSON-RPC v2.0）。
- 金额、费率、时间等关键参数必须有业务范围校验。
- 核心操作（转账、签名、权限变更、参数配置）必须记录审计日志。
- 禁止引入业务外功能（如仅需 ERC20，禁止额外实现 ERC721）。

## 5. 日志与可观测性
- 日志必须包含操作人/时间/结果/traceId（如有）。
- 禁止记录私钥/助记词/API Key 等敏感数据。
- 错误使用带上下文的封装（Go 使用 `%w`；Solidity 使用自定义错误）。

## 6. 测试与校验（强制）
- 编译校验：Go 使用 `go build -mod=readonly`；Solidity 使用 `solc 0.8.20+`。
- 安全扫描：Go 使用 `gosec`/`golangci-lint`；Solidity 使用 Slither/Mythril。
- 覆盖率：核心业务逻辑测试覆盖率 ≥90%。
- 性能/Gas：关键路径性能或 Gas 消耗不高于同类实现的 110%。

## 7. 变更与提交规范
- 变更需与需求/业务规则一一对应，避免范围膨胀。
- 提交信息清晰描述范围与影响，禁止混合无关改动。
- 任何安全相关变更必须经过审计或复核。

### 7.1 提交规范（Conventional Commits）
- 格式：`<type>(<scope>): <subject>`。
- type 允许：`feat` `fix` `chore` `docs` `refactor` `test` `perf` `build` `ci` `revert`。
- scope 需为具体模块或领域，如 `contracts` `sdk` `backend` `infra` `docs`。
- subject 使用祈使句，中文或英文均可，长度 ≤72 字符，不以句号结尾。
- 一次提交只做一类改动，禁止跨域混杂（例如代码+大量格式化+依赖升级）。
- 影响安全/协议/资产的变更需在提交说明中补充风险或审计点。

## 8. 交付与文档
- 关键业务流程需有简要说明（输入/输出/失败场景）。
- 对外接口需注明参数校验规则与错误语义。
- 生成内容必须可复现、可审计、可验证。

---
本文件为主项目统一规范，所有新代码与自动生成内容必须遵守。
